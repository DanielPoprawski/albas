target/
# Holds ALBAS_SYNC_TOKEN — the only credential the sync server has.
.env
*.db
*.db-wal
*.db-shm

# The live nginx config is a copy of bootstrap.conf or tls.conf, made on the
# server — conf.d/ is what compose mounts, nginx/ is what git tracks.
nginx/conf.d/
