/**
 * The phone header's square icon button (Settings, the calendar-view picker).
 * Shared by `HomeView` and `CalendarNav` so the two corners of the bar match.
 */
export const MOBILE_HEADER_BUTTON =
  'flex size-8 shrink-0 cursor-pointer items-center justify-center border-0 bg-subtle p-0 text-ink transition-all active:bg-line';
